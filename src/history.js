// The history package was installed automatically with react-router-dom.
// It is a dependency of react-router-dom.

import { createBrowserHistory } from 'history';

export default createBrowserHistory();